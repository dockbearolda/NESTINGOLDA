{
  "name": "dasholda-atelier",
  "version": "1.0.0",
  "private": true,
  "description": "Outil interne atelier Dasholda",
  "scripts": {
    "build": "esbuild BAXK/app.js --bundle --target=chrome70,safari12,firefox78 --format=iife --outfile=BAXK/app.bundle.js",
    "start": "npm run build && node server.js",
    "dev": "npm run build && node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
